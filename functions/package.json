{
  "name": "live-translator-functions",
  "private": true,
  "description": "Firebase Cloud Functions for Sayvo (Gemini ephemeral token minting).",
  "engines": {
    "node": "22"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "@apple/app-store-server-library": "^3.1.0",
    "firebase-admin": "^14.4.0",
    "firebase-functions": "^6.4.0",
    "google-auth-library": "^11.1.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^3.0.0"
  }
}
